[pytest]
; # Specify the directories where pytest should look for tests
testpaths = 
    tests

; # Add the backend directory to the Python path so that tests can import backend modules
python_paths = 
    backend

; # Configure asyncio settings
asyncio_default_fixture_loop_scope = function

; # Register custom markers
markers = 
    asyncio: Mark test as asyncio